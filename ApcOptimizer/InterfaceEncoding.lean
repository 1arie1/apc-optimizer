-- The interface-encoding metatheory: a standalone justification of the SMT verifier's
-- interface memory encoding. `Equiv` states the abstract/concrete equivalence notions,
-- `Transfer` proves abstract ⇒ concrete.
import ApcOptimizer.InterfaceEncoding.Equiv
import ApcOptimizer.InterfaceEncoding.Transfer
